	Тип переменных
string 
char
int
uint(u) (положительные)
long(l)
ulong(ul) (положительные)
float(f)
decimal(m) - для хранения очень больших чисел и с огрмной точностью (длядробных чисел)
byte - цифры от 0 до 255
sbyte - цифры от -128 до 127
short - цифры от -32768 до 32767
ushort - цифры от 0 до 65535
bool
var - тип-трансформер (любой тип переменных)

	Консоль
Console.WriteLine("Вывожу строку") - вывод в консоль с перенсом
Console.Write("И еще одну") - вывод в консоль без переноса
Console.WriteLine($"Меня зовут {name} и мне {age} лет!"); - для вывода переменных сп1 (Прием: интерполяция)
Console.WriteLine($"Меня зовут {1} и мне {2} лет!", name, age); - для вывода переменных cп2 (Прием: конкатенация)

string name=Console.ReadLine(); - ввод с клавиатуры
int age=int.Parse(Console.readline()) - parse - конвертация со  string в int (в дфнном случае)
var key=console.readkey().key - считывание кнопки клавиатуры
	if(key==ConsoleKey.W)  

	Логические операции
bool c=a>=b - c = true если a>=b
^ - операция исключющего или

	Перевод переменных в другие типы:
Convert.ToInt32() - перевод в int
	

